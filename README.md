# Photogenic

Cross-platform, AI-assisted photo editor (see `ARCHITECTURE.md` and `docs/adr/`).

## Current implementation slices
- Phase 0 viewport-proof contract + browser harness with readable evidence reporting, including shell-webview DOM measurements for zoom/pan + overlay behavior and a requestAnimationFrame FPS sample when the Tauri bridge is available
- Viewport-proof session persistence seam for saved shell evidence snapshots
- Viewport-proof workflow seam for collect/save/load report cycles
- Viewport-proof foundation seam for deterministic proof-summary metadata shared by workflow/report/dashboard paths
- Viewport-proof dashboard workflow seam for saved run summary composition
- Edit Recipe + Catalog foundation seam
- Sidecar roundtrip + conflict-policy seam
- Sidecar workflow seam for export/import sync inspection
- Sidecar dashboard foundation + workflow seam for saved sidecar-link sync-state summaries
- Import workflow seam for supported RAW/JPEG/TIFF/PNG ingestion
- Library inventory + ratings/flags/filtering foundation seam
- Catalog workflow seam wiring import + default recipe persistence
- Preset + Batch Sync persistence/apply seam
- Preset workflow seam for source-independent presets + batch-sync application
- Batch-session persistence/workflow seam for deterministic batch-sync session save/load/apply
- Workspace-session persistence seam for deterministic reopen state snapshots
- Workspace-session workflow seam for validated reopen context restore
- Catalog dashboard workflow seam for reopen-state/library summary composition
- Deterministic preview/proxy + preview/export parity foundation seam with shared software PNG rendering
- Proxy cache foundation seam with deterministic persistence and LRU-style eviction
- Preview workflow seam composing preview request lifecycle with proxy-cache hit/store metadata
- Preview-session persistence/workflow seam for saved preview request/cache snapshots + deterministic reload summaries
- Preview dashboard workflow seam for saved preview session summaries
- Export format-options normalization seam
- Export naming-template foundation seam
- Export batch-queue foundation seam
- Export workflow seam that queues deterministic artifact exports from naming templates
- Export-session persistence seam for deterministic export job snapshots
- Export-session workflow seam that saves and reloads queued/running/done/failed export jobs deterministically, refreshing placeholder-proof companion file presence for restored done jobs
- Export dashboard foundation seam for deterministic saved job summary metadata
- Export dashboard workflow seam for saved job summary composition
- Local License vs cloud Credit entitlement seam
- Licensing foundation seam for deterministic access-summary metadata from local-license/cloud-credit inputs
- Licensing workflow seam for local-license vs cloud-credit access decisions
- Licensing snapshot persistence/workflow seam for saved access snapshots + summarized reports
- Licensing dashboard foundation seam for deterministic saved licensing snapshot summary metadata
- Licensing dashboard workflow seam for saved access snapshot summaries
- Ordered local issue breakdown in `.scratch/photogenic-foundation/issues/`
- Tauri workflow prep via `npm run tauri:attempt`, `npm run tauri:dev`, and `npm run tauri:build`

## Tauri shell next step
On a Cargo-capable machine, restore or regenerate the real `src-tauri/` scaffold and commit the source/config subset:
- `src-tauri/.gitignore`
- `src-tauri/Cargo.toml`
- `src-tauri/Cargo.lock`
- `src-tauri/build.rs`
- `src-tauri/tauri.conf.json`
- `src-tauri/capabilities/default.json`
- `src-tauri/src/**/*.rs`
- `src-tauri/icons/*`

Keep generated outputs ignored and out of commits:
- `src-tauri/target/`
- `src-tauri/gen/schemas/`

## Scope honesty
- In this repo checkout, the real `src-tauri/` scaffold is now present as untracked/generated project files on the local machine and should be reviewed before commit.
- `npm run tauri:attempt` is the honesty gate for whether the local machine can actually run Tauri work.
- `npm run tauri:dev` and `npm run tauri:build` are the standard workflow once the scaffold is present.
- The browser harness now reports measured viewport-proof evidence gate-by-gate so placeholder UI state is readable and cannot masquerade as a passed shell proof.
- When the Tauri shell bridge is available, the harness now merges shell-reported window metrics with measured harness-DOM zoom/pan + overlay checks, a shell-webview DOM color sample with persisted RGBA metrics, and a real requestAnimationFrame-based sustained-fps sample while still leaving raw-frame provenance explicitly unproven until separately measured.
- The browser harness gradient remains a placeholder and does not count as the real GPU→webview gate from ADR-0004.
- The preview seam now models queued/cancelled/superseded/ready request states, the preview workflow composes that lifecycle with proxy-cache hit/store metadata plus deterministic software-rendered PNG cache writes on disk, re-validates cached PNG files before serving a hit, treats parseable-but-mismatched PNG bytes as stale cache data instead of valid hits, and the preview-session seam persists those request/cache snapshots so future UI restore flows can reload one or many saved preview states, refresh rendered-cache metadata from disk, optionally auto-repair broken ready-cache files during reload, repair broken ready-cache files explicitly through the same seam, dedicated repair-all helpers can trigger that bulk repair flow directly, optionally scoped to selected saved previews, summarize bulk reload repair results including remaining rendered-cache issue types plus requested/processed/skipped counts, the repaired/unresolved session ids, and any skipped unknown selections, and surface latest queued/cancelled/superseded lifecycle examples, latest hit/miss/stored cache examples, plus present/missing/invalid/stale rendered-cache examples without inventing hidden cache history; this is real preview image-file output for the seam, but background generation and the final RAW/GPU pixel pipeline are still future work.
- The export seam now validates format/quality/resize/ICC/sharpening options deterministically at artifact-write time; authoritative preview↔export parity is proven by native pipeline recipe-fingerprint identity and native pixel fixtures, so behaviorSignature is retained on artifacts as a diagnostic only and is no longer the parity gate.
- The import workflow seam now classifies supported image formats and writes deterministic library inventory records, including observable filesystem metadata such as file size and modified time plus readable PNG/JPEG/TIFF-like header metadata (pixel dimensions, EXIF-lite orientation, observed capture timestamp, and camera make/model plus lens model, focal length, aperture, shutter speed, and ISO when present) when source files are available, plus aggregate import counts/byte totals for those observable source files; broader EXIF extraction and SQLite ingest remain future work.
- The library store now covers image inventory + culling metadata plus observable source-file metadata at the same black-box persistence seam as the catalog recipe store, including refresh of byte-size/modified-time observations, readable dimensions, JPEG EXIF-lite orientation, observed capture timestamp, and camera make/model plus lens model, focal length, aperture, shutter speed, and ISO from disk for existing imports; the lower-level source-file-insights seam can also summarize mixed source paths directly for maintenance/reporting flows, including latest observed capture, latest observed-format, latest observed-camera, latest observed-lens, latest observed-focal-length, latest observed GPS-coordinate, and latest observed-exposure examples (aperture/ISO/shutter speed plus flash-fired observation and exposure-bias EV) plus a report helper with requested/observed/missing path tracking. SQLite/import indexing and deeper EXIF extraction are still future work. The library listing seam can now also filter persisted entries by observed format, camera make/model, lens model, flash-fired state, GPS-presence, exact metering/white-balance mode, minimum/maximum GPS altitude, and minimum/maximum ISO, focal length, aperture, shutter speed, and exposure-bias EV for catalog-side navigation/reporting flows, supports tokenized free-text query across persisted catalog metadata with all-term or any-term matching and optional field scoping, can sort filtered results by rating, timestamps, file size, pixel area, ISO, or focal length, and supports limit/offset pagination over the sorted filtered result set.
- The Tauri shell now exposes a viewport-proof command bridge so the harness can distinguish shell-connected provisional evidence from plain browser fallback, persist structured shell/webview metrics (physical size, scale factor, FPS sample duration/frame count), and report them honestly; real raw-frame GPU provenance is still future work.
- The viewport-proof verdict now exposes `fallbackActivated` plus `measuredGateFailures`, so the ADR-0004 fallback ladder is activated by code on a measured hard-gate failure (distinct from provisional/not-measured evidence, which never activates fallback per ADR-0010); the persisted workflow path also forwards the zoom/pan, overlay, color, and sustained-fps measurements so a saved session can capture every gate, not just gradient and raw_frame.
- Production preview/export construction (`requireNativePipeline`) now surfaces `native-unavailable` when the native Pipeline cannot be reached, instead of silently software-rendering; the deterministic JS renderer remains reachable only as an explicit test fallback.
- The export seam now includes deterministic file-naming template rendering so batch/export artifacts can agree on naming inputs before real pixel writes exist.
- The export seam now includes an in-memory batch queue foundation for deterministic job ordering and explicit failure states; real parallel file writes remain future work.
- The export workflow seam now composes naming templates + queueing into JSON artifact sidecars plus companion output files on disk, recording companion path/type/size plus sha256 integrity metadata while staying explicit that JPEG, PNG, and TIFF-16 writes are deterministic software-rendered image bytes rather than the final rendered RAW/GPU pipeline; resize dimensions, the sharpen-for-output flag, and the embed-ICC flag now affect those seam-level rendered bytes. Matching preview-cache PNGs and PNG export companions now share the same deterministic renderer seam and produce byte-identical output when source/recipe/dimensions match.
- The catalog workflow seam now wires supported-file import into both library inventory and default recipe persistence while delegating culling metadata updates to the library store, can refresh one or many imported source-metadata observations with operation metadata plus aggregate coverage summaries for observed format/capture/camera/lens/focal-length/exposure coverage including aperture/ISO/shutter speed, can list persisted library entries through the same seam using culling and observed-metadata filters plus tokenized free-text metadata query with all-term or any-term matching and optional field scoping with both page-level and total-matched metadata summaries, page-level and total-matched library facet summaries (rating/color-label/flagged/rejected/keeper coverage), plus total-matched/limit/offset pagination metadata, next/previous page info, derived page-size/page-count/current-page metadata, and explicit visible/matched image-id lists, and now also exposes a summary-only list-library helper for callers that need counts/facets without returning the page entries themselves; and can inspect arbitrary source paths through the lower-level source-file-insights seam without importing them first, including observed format/capture/camera/lens/focal-length/exposure metadata when readable, including aperture/ISO/shutter speed plus flash-fired observation and exposure-bias EV when readable; full SQLite/app UI flows remain future work.
- The preset workflow seam now composes stored source-independent presets with batch-sync application across multiple target recipes.
- The batch-session seam now persists deterministic batch-sync sessions (source image, included operation types, target image ids, timestamps) and reapplies them through the existing recipe batch-sync helper; app UI remains future work.
- The licensing foundation seam now extracts deterministic license-status/credit-balance metadata from entitlement snapshots so workflow, session, and dashboard summaries can share one honest access-summary source.
- The licensing workflow seam now summarizes local-edit/local-export access separately from cloud-generative credit access so offline license state and cloud balance cannot be conflated in future UI flows.
- The licensing snapshot seam now persists local-license/cloud-credit snapshots and reloads a stable summarized access report through the existing licensing workflow.
- The sidecar workflow seam now composes catalog export/import with explicit sync inspection so future UI flows can distinguish in-sync, conflict, and missing-catalog states without bypassing the catalog source-of-truth policy, while also surfacing observed sidecar file metadata, persisted sidecar-link timestamps, sidecar freshness relative to link time, plus catalog↔sidecar revision-drift metadata across export, import, and sync-inspection flows.
- The sidecar dashboard foundation seam now centralizes deterministic saved sidecar sync summary metadata from linked recipe entries and inspected sync states so workflow/dashboard surfaces can share one honest latest-sync/counts helper, including counts of sync states with observed sidecar files, latest and largest observed sidecar-file examples, revision-drift tallies, sidecar freshness tallies (including unknown freshness), latest matched/catalog-newer/sidecar-newer revision examples, latest unchanged/modified-after-link freshness examples, plus aggregate observed sidecar-file byte-size/modified-time metadata.
- The sidecar dashboard workflow seam now delegates saved catalog↔sidecar link summary shaping through that foundation while keeping in-sync/conflict/missing-sidecar counts, aggregate sync-issue counts, aggregate revision-issue counts, and aggregate freshness-issue counts stable for future UI surfaces and surfacing aggregate observed sidecar-file metadata, explicit present/missing observed sidecar-file counts and latest examples, linked-with/without-timestamp counts, latest linked image ordering, earliest/latest linked-with-timestamp examples, linked timestamp range/span metadata, explicit linked-without-timestamp fallback visibility, missing-after-link freshness visibility, and explicit unknown-revision-drift summary visibility, plus latest revision-drift examples from inspected links.
- The workspace-session seam now persists deterministic reopen snapshots (selected image, active filter, preset/session references, expanded image ids) so future UI flows can restore session context without inventing hidden state.
- The workspace-session workflow seam now validates selected-image/preset/batch-session references before saving and can summarize reopen context for future UI restore flows.
- The viewport-proof session seam now persists shell evidence snapshots with computed ADR-0004 verdicts so future Tauri/browser tooling can save and compare measured proof runs instead of relying on ephemeral output.
- The viewport-proof workflow seam now composes shell-result collection, persistent session snapshots, and readable reports so future Tauri/browser tools can save proof runs and reload the exact ADR-0004 verdict state.
- The viewport-proof foundation seam now centralizes deterministic proof-summary metadata from gate results/verdicts so workflow/report/dashboard surfaces share one honest ADR-0004 status source.
- The export-session seam now persists deterministic export job snapshots (image/output/status/options/error/timestamps plus source/recipe restore data) so future UI flows can restore batch/export history without pretending image bytes were written here.
- The export-session workflow seam now composes queued artifact exports with saved session snapshots so queued/running/done/failed jobs can be reloaded deterministically into a fresh in-memory export workflow, including refreshed JPEG/PNG/TIFF rendered-image companion metadata for restored done jobs so missing-on-disk, invalid, or parseable-but-stale rendered companion files surface honestly without claiming the final RAW/GPU pipeline exists; invalid and stale TIFF companions now surface through the same reload/dashboard integrity path, and done JPEG/PNG/TIFF jobs can also be rerun through the same seam to repair broken companion/sidecar files back to a healthy rendered-image state, reloads can optionally auto-repair done-job integrity issues through that same rerun path, dedicated repair-all helpers can trigger that bulk repair flow directly, optionally scoped to selected saved jobs, and bulk reload reports can summarize how many jobs were repaired vs still have integrity issues including remaining proof/sidecar issue types plus requested/processed/skipped counts, the repaired/unresolved job ids, and any skipped unknown selections.
- Export dashboard/session summaries now surface whether a restored done JPEG/PNG/TIFF rendered-image companion is present/missing/invalid/stale on disk, including missing/invalid/stale TIFF proof visibility plus missing/invalid/stale TIFF reload/repair summaries through the same dashboard workflow seam, whether its JSON artifact sidecar is present/missing/invalid/stale, the latest example of each proof/sidecar state, and the latest queued/running/done/failed job examples, but this remains deterministic seam-level output bookkeeping rather than final rendered-image verification.
- The catalog foundation seam now centralizes deterministic dashboard-summary metadata shaping so catalog workflow/dashboard surfaces can share one honest workspace/library summary helper, including visible-source byte totals, counts of images with known file metadata, latest observable modified timestamps, and selected-image source-file details when library metadata is available.
- The catalog dashboard workflow seam now composes workspace reopen state, library filters/counts, and active preset/batch-session references into a single summary for future culling/develop UI surfaces, can surface latest/largest visible source-file examples, latest visible observed-capture source-file visibility, latest visible observed-format source-file visibility, plus latest incomplete-metadata source-file visibility alongside selected-image source metadata, can optionally refresh observable source-file metadata from disk before producing that summary (including selective refreshImageIds targeting), can list the currently visible workspace library through the active workspace filter using tokenized metadata query/sort/pagination with visible/matched ids, current page index/number metadata plus current page image ids, current page first/last image ids, and matched-set start/end positions, explicit selected-image existence/visibility/GPS-presence/GPS-altitude-presence/metering-mode-presence/white-balance-presence/exposure-program-presence/current-page containment/current-page selected-image id/current-page selected-image index/current-page selected-image position/match state metadata plus a current-page selection-state enum and grouped current-page selection summary object with explicit hasSelection and hasMatchedSelection state, the snapshot-selected image id, and matched position/page-offset/page-limit/page-size/has-results/is-paged/page-index/page-number/total-pages/previous-next-page/previous-next-page-number/previous-next-offset/range/count/single-image/edge/image-id metadata plus the current visible page offset/limit/page-size/is-paged/index/number/total-pages/range/total-matched-count/result-count/has-results/previous-next-page/previous-next-page-number/previous-next-offset/single-image/edge/image-id/current-page-edge-id slice for the grouped selection summary when the selected image participates in the filtered result set, selected-image matched offset/position/page metadata plus selected-image page start/end/count/edge/image-id metadata and matched-set before/after counts, on-page selected-image index/position, page-local before/after counts, page-local neighboring ids, and neighboring matched image ids, can jump directly to the selected image's containing page for paged listings and reports whether or why that selection anchor adjusted, skipped, or left the requested page offset unchanged, including the selected page's image-id set, page count/number, and first/last page image ids, plus page/matched summaries and facets, and now exposes both a bulk workspace source-metadata refresh report with requested/refreshed/skipped ids plus aggregate coverage counts and convenience helpers for refreshed-summary and summary-only visible-library listing for future maintenance tooling.
- The export dashboard foundation seam now centralizes deterministic export job summary metadata so workflow/dashboard surfaces can share one honest saved-session summary helper.
- The export dashboard workflow seam now delegates saved export-session history shaping through that foundation while keeping counts, latest job ordering, recent failures, aggregate proof-issue, artifact-sidecar-issue, and overall integrity-issue counts, latest proof/state examples stable for future export UI surfaces, and TIFF rendered-image reload/repair summaries, including stale-proof repair cases, flowing through the same dashboard seam as PNG.
- The viewport-proof dashboard foundation seam now centralizes deterministic saved proof-session summary metadata from normalized proof summaries/verdicts so workflow/dashboard surfaces can share one honest latest-proof/counts helper.
- The viewport-proof dashboard workflow seam now delegates saved proof-session history shaping through that foundation while keeping unlocked/provisional counts, aggregate proof-issue counts, measured-vs-unmeasured run counts, measured-proof-issue visibility, browser-vs-shell run counts, latest browser/non-browser examples, latest measured-run visibility, latest unlocked/provisional runs, and a best-progress summary stable for future shell tooling.
- The licensing dashboard foundation seam now centralizes deterministic saved licensing snapshot summary metadata from normalized entitlement summaries so workflow/dashboard surfaces can share one honest latest-access/counts helper.
- The licensing dashboard workflow seam now delegates saved licensing snapshot history shaping through that foundation while keeping active/inactive, credit-balance, active/inactive × credit-presence summaries, and aggregate access-issue visibility stable for future account/export UI surfaces and surfacing latest with-credits/without-credits examples, latest active-with/without-credits examples, latest inactive-with-credits example, latest access-issue example, plus the highest observed credit-balance snapshot alongside latest active/inactive snapshots.
- The preview dashboard foundation seam now centralizes deterministic saved preview-session summary metadata so workflow/dashboard surfaces share one honest latest-preview/counts helper.
- The preview dashboard workflow seam now delegates saved preview-session history shaping through that foundation while keeping request-state/cache-state counts, aggregate cache-issue counts, aggregate rendered-cache issue counts, plus latest ready/queued previews and present/missing/invalid/stale rendered-cache state examples stable for future culling/develop UI surfaces.
- The catalog store layer now accepts a pluggable SQLite-backed `catalogBackend` (`src/catalog/sqlite-backend.js`) so existing recipe, library, preset, and workspace-session workflows can run against durable SQLite storage (via node:sqlite) instead of only JSON files; recipe/library/preset/workspace entries survive process restart, folder import writes one durable image row per supported RAW/JPEG/TIFF/PNG source without duplicates, and metadata refresh updates file size/modified time in-place. In production, these workflows delegate to the Rust rusqlite store via Tauri commands; in tests they use the node:sqlite adapter directly. The Rust SQLite schema and the JS adapter agree on the same table names at the catalog boundary.
- The offline licensing layer now supports Ed25519-signed license activation (`src/licensing/license-key.js`, `src/licensing/activation.js`): a signed license is cryptographically verified against an embedded public key, status/expiry windows are checked, and the verified license is cached as an offline snapshot. Cloud credits are explicitly ignored for local-edit/local-export access and cannot rescue an expired or missing license. A non-throwing `describeExportLicensingState` helper returns active/inactive/expired/no-license state with a human-readable reason so future export controls can explain licensing state without blocking library workflows.
- The export batch engine now runs queued jobs with bounded concurrency, per-job failure isolation, and AbortSignal-based cancellation (`runBatch` on `createExportWorkflow`). JPEG quality changes encoded output bytes, TIFF writes a real 16-bit file, ICC embed flag is recorded in artifact metadata, and batch summaries preserve deterministic enqueue ordering. A failed job does not stop unrelated jobs; cancellation prevents queued jobs from starting.
- A platform smoke script (`scripts/smoke.mjs`, `npm run smoke`) exercises the full alpha workflow chain — Tauri availability, Pipeline capabilities, Ed25519 license activation, SQLite-backed import, culling, recipe develop, preset create/apply, batch sync, JPEG batch export, and viewport-proof summary — emitting explicit pass/fail fields per step and writing a JSON report to `.scratch/photogenic-foundation/verification/smoke-<platform>.json`. A cross-platform packaging runbook documents macOS/Windows/Linux build prerequisites, GPU/CPU fallback expectations, and report locations.
- The native decode layer now decodes real PNG and TIFF files into linear float RGB samples (`src-tauri/src/core/decode.rs`). PNG decode uses the `png` crate; TIFF decode is a pure-Rust implementation supporting uncompressed baseline RGB (8-bit and 16-bit, little-endian and big-endian). `render_pipeline` uses real decoded pixels when a source path is provided and no JS-supplied samples are passed. RAW formats (NEF/CR2/ARW/DNG/RAF) and JPEG still return a 1×1 placeholder pending dedicated decoders, but the decode→CPU-pipeline→render path is now proven with real image pixels for two formats.
- The editor UI shell replaces the Phase 0 viewport harness with a real editor layout: library sidebar (imported image grid), center preview area, right sidebar with develop controls covering all recipe operation types (exposure, temperature, tint, contrast, highlights, shadows, whites, blacks, tone curve, HSL red channel, sharpening, noise reduction, crop, rotate, straighten), batch-sync panel with explicit operation-type selection, preset save/load/apply, and export panel with licensing gate. A Tauri command bridge (`app/tauri-bridge.js`) wraps the IPC so the UI degrades honestly to a “backend disconnected” state when Tauri is not available. Ten Tauri commands expose the SQLite catalog store to the webview via managed `Mutex<SqliteCatalogStore>` state: `list_library`, `get_recipe`, `save_recipe`, `list_presets`, `save_preset`, `apply_preset`, `get_workspace_state`, `save_workspace_state`, `batch_sync`, `check_license`, `update_culling`, `list_culling`, `export_image`, `import_images`. The `export_image` command decodes a real PNG/TIFF/JPEG source, applies the CPU pipeline, encodes the result as an 8-bit sRGB PNG (with recipe fingerprint provenance) or an uncompressed TIFF (8-bit or 16-bit), and writes it to disk. The `import_images` command imports source files into the managed catalog store with format classification and deduplication. The editor layout is verified rendering in headless Chrome with 19 develop controls, live preview rendering via `render_pipeline` with stale-request supersession, culling metadata persistence, export execution through the pipeline with licensing gate, and import via text-input path entry — all independently tested.
