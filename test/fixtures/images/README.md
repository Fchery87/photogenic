# Image Fixtures

Small source fixtures for native decode boundary tests.

- `minimal.nef` is a deliberately tiny placeholder RAW fixture. It is not a real
  camera RAW; Task 1.3 uses it to prove the native decode adapter boundary and
  typed decode result shape before a LibRaw or rawler implementation is selected.
